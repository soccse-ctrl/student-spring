CREATE TABLE IF NOT EXISTS students (
                                        id SERIAL PRIMARY KEY,
                                        name VARCHAR(100),
    age VARCHAR(100),
    course VARCHAR(100)
    );
